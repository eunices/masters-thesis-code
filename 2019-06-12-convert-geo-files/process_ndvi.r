# Libraries
library(raster)
library(maptools)
library(MODIS)

# Params
directory = 'data/geo'
map_crs = '+proj=longlat +datum=WGS84'

# Initialize
data(wrld_simpl)
# Libraries
library(raster)
library(maptools)


# Params
directory = 'data/geo'
map_crs = '+proj=longlat +datum=WGS84'

# Initialize
data(wrld_simpl)


################ NDVI layers ################

